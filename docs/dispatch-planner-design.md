# Дизайн: суточный capacity-планировщик рассылки

**Статус:** черновик v1 (2026-07-09). Согласовано: каденс — непрерывный; рендер HTML — ленивый; цель заявки — офферы (мин. 4) с режимом max_reach. Открытые вопросы — в конце.

## 1. Проблема (зачем)

Сейчас **генератор жадный**: `emails:generate-queue` сразу рендерит HTML на всех поставщиков батча в `email_queue(pending)`. **Диспетчер душит** пост-фактум (лимит ящика + пейсинг + дневной потолок получателя). Разница копится в `pending` → **пробка**. Волны v2, held-пул и 4 реактивных деферрала (`deferred_batches`: sender_capacity / recipient_load / quality-gate, + followup) — костыльное приближение планирования; система борется сама с собой.

**Первопричина ожога доменов** (см. `memory/iqot-deliverability-regression.md`): на хендовере n8n→Laravel сняты неявные рейт-лимиты → бёрст скорости домена (1680/час) + вал на получателя (117/день с 82 ящиков → FBL-жалобы). Ёмкость должна быть **входом планирования**, а не тормозом.

## 2. Ключевая идея — разделить BACKLOG и OUTBOX

| Уровень | Что | Стоимость | Живёт |
|---|---|---|---|
| **Backlog спроса** | «интент»: кому хотим написать по какой заявке (request×supplier), с рангом/тиром | дёшево (строка) | копится сколько угодно, не протухает |
| **Outbox** | отрендеренное письмо, готовое уйти (`email_queue`) | дорого (AI-тело + HTML) | рендерим **лениво**, только под то, что влезает в ёмкость |

Из всего редизайна это — главное. Пробка исчезает by design: рендерим ровно то, что уйдёт. Волны/приоритет/обратная связь становятся *следствием*, а не отдельными механизмами.

## 3. Сущности и ёмкости

**Единица работы — ПОЗИЦИЯ (`request_item`), НЕ батч.** Мы не ограничены батчами: ведём список активных позиций. У каждой — своя цель покрытия и своё состояние. Позиция, закрытая «с лихвой», **выпадает из работы глобально** → освобождает ёмкость получателей/сендеров под другие позиции (нет смысла её дальше слать).

**Спрос:** `send_intents` — гранулярность **(request × supplier)**, но payload = АКТИВНЫЕ позиции этой заявки, матчащие поставщика (на момент рендера). Поля: request_id, supplier_id, tier (hot/warm/cold), score, status (backlog|rendered|sent|dropped). Источник: подбор пула (`CampaignSupplierSelector`) + discovery. Тир = качество матча (Яндекс-hot → warm-добор → cold-пул).

**Батчей упреждающих нет.** «Батч ≤5» — только ограничение РАЗМЕРА письма: на рендере берём активные позиции поставщика и, если их >5, режем на несколько писем. Список позиций динамический (только ещё-не-закрытые) → закрытая позиция автоматически перестаёт попадать в новые письма.

**Ёмкости (на день, МСК):**
- **Сендер:** `SenderDailyCapacity` — `warmup daily_limit − израсходовано` (переиспользуем как есть).
- **Получатель:** адаптивный дневной cap (см. §5). Anchor — `recipient_mailboxes` (уже есть `daily_sent_count/date`).
- (опц.) **Глобальный потолок** платформы/сутки.

## 4. Формула приоритета (score интента)

```
score(intent) =
    w_deficit  * deficit_factor(request)      // главный драйвер
  + w_customer * is_customer_request
  + w_age      * age_factor(request)          // старые не должны голодать
  + w_tier     * tier_weight(hot|warm|cold)   // hot вперёд
  + w_rating   * supplier_rating_norm
```

**Покрытие считается ПО ПОЗИЦИИ** (не по заявке в среднем). Позиция **satisfied**, когда набрала ≥ `offer_target` (мин. 4) РАЗНЫХ ценовых ответов (переиспользуем логику `coverageForBatch` → на уровень позиции). Satisfied-позиция выпадает из активного набора → её больше не включаем в письма.

**deficit_factor(request)** = агрегат по ещё-активным позициям заявки:
- **target** (дефолт, `offer_target≥4`): по каждой активной позиции `deficit_i = max(0, offer_target − offers_i)`; фактор заявки `= (Σ deficit_i / Σ offer_target)` по активным позициям. Заявка полностью **satisfied**, когда активных позиций не осталось (все закрыты) — интенты выбывают.
- **max_reach** (флаг заявки): фактор держится высоким (константа), пока **пул кандидатов не исчерпан** — шлём всем, не останавливаясь на 4; позиции не «закрываются» досрочно.

Интент (request×supplier) «живой», пока у поставщика есть ≥1 активная матчащая позиция, ещё не отправленная ему.

«Волны» = следствие: сначала в план проходят hot-интенты (высший tier_weight); заявка недобрала → на следующих тиках проходят warm/cold того же реквеста. Никакого held-пула и таймерных волн.

## 5. Адаптивный дневной cap получателя

- База **10**/день (в т.ч. для нового адреса без истории). Позитивный сигнал (ответил / прислал оффер, нет баунсов) → рост **до 15** постепенно. Отсутствие реакции / рост баунсов → спад **до 5**. Жёсткий баунс/жалоба → блок (`is_blocked`, уже есть).
- Хранение: расширяем `recipient_mailboxes` (текущий `daily_sent_count/date`) полями `cap_current`, сигналы вовлечённости. Пересчёт — в обратной связи (§7).

## 6. Планировщик (непрерывный top-up)

Каждый тик (диспетчер и так поминутно):
1. Посчитать остаток ёмкости: по каждому сендеру и получателю на сегодня (+ глоб. потолок).
2. Взять из backlog топ-N интентов по `score`, у которых **satisfied=false**.
3. Жадно назначить: для интента — если у его получателя есть остаток дневного cap И есть подходящий сендер (правило org/анти-фингерпринт) с остатком → **отрендерить письмо (лениво)** и положить в `email_queue(pending, scheduled=now)`. Иначе — пропустить, интент остаётся в backlog.
4. Дальше — существующий диспетчер (пейсинг/слоты/отправка) без изменений.

«Суточный план» = *приоритет + бюджет ёмкости*; **исполнение непрерывное**. Экспресс-полоса: клиентские/срочные заявки — повышенный `w_customer` (проходят вперёд), опц. в обход накопления.

Ленивый рендер: тело батча (greeting/intro/ai_request, `CampaignBodyGenerator`) генерим 1 раз на батч и кешируем; per-supplier `CampaignEmailBuilder` — дёшево, на момент выбора интента.

## 7. Обратная связь (утро/непрерывно)

- **Доставка:** баунсы/спам-реджект по получателю → снижаем его cap / блок; по домену-отправителю → сигнал притормозить/отлежаться.
- **Вовлечённость:** ответ/оффер → поднимаем cap получателя (до 15).
- **Цель заявки:** пересчёт `offers_received` + позиционного покрытия → satisfied → интенты выбывают (target); max_reach — до исчерпания пула.
- **Приоритеты:** недобравшие заявки всплывают (deficit растёт с возрастом).

## 8. Что переиспользуем / ретайрим

**Переиспользуем:** `SenderDailyCapacity`, `CampaignSupplierSelector`/ранжирование, discovery, `CampaignBodyGenerator`+`CampaignEmailBuilder`, `recipient_mailboxes` + дневной cap, весь низ диспетчера (пейсинг/слоты/анти-бан).

**Ретайрим (сливается в планировщик):** waves_v2 + held-пул, `deferred_batches` (load/capacity/quality), `ProcessLoadDeferred`/`ProcessCapacityDeferred`/`RetryDeferred`, `DispatchFollowupEmails`, `WarmupBatchSplitter`.

## 9. План миграции (без big-bang, за флагом)

- **Фаза 0 (сделано):** пер-получательский дневной потолок (`recipient_daily_cap`), warmup age-cap.
- **Фаза 1 (СДЕЛАНО):**
  - (а) ✅ **адаптивный cap получателя** (10/15/5, `recipient_mailboxes.daily_cap`, `emails:recompute-recipient-caps` 04:45 МСК). Коммит 904004e.
  - (б) ✅ **рекапенет-гейт в генерацию** (за флагом `EMAILS_GENERATE_RECIPIENT_GATE`, дефолт OFF): не рендерить письмо получателю с переполненным near-term outbox (≥ cap не-held pending) — снятые (волны 1–2) откладываются в capacity-backlog (reason=recipient_cap, пин) и повторяются `emails:process-capacity-deferred`. Проверено рефлексией. Коммит 18d7ecb. **Каветат:** повтор пересоздаёт deferred-строки (churn), пока outbox не разгрузится — приемлемо как первый кирпич; Фаза 2 заменяет на чистый backlog интентов.
- **Фаза 2 (В РАБОТЕ, за флагом `EMAILS_PLANNER_ENABLED`=OFF, параллельно текущему):**
  - Шаг 1 ✅ схема `send_intents` + `requests.offer_target/max_reach`.
  - Шаг 2 ✅ `PositionCoverage` (позиционное покрытие: активные позиции, satisfied, priced-counts).
  - Шаг 3 ✅ `emails:build-intents` — билдер backlog (позиционно, grouper+selector, дедуп). Проверен.
  - Шаг 4 ✅ `emails:plan-render` (PlanRenderIntents) — ленивый рендер backlog под ёмкость: приоритет (клиентские/старые вперёд), capacity-gate получателя (adaptive cap + не-held pending) с разносом 1/получатель/прогон, рендер активных позиций (переиспуя SenderAssigner/Token/Body/EmailBuilder/Persister), интент→rendered, остаток→backlog. Всё волна 1 (held-пула нет). `--dry-run`. Проверено E2E (442→рендер 2/backlog 6). Расписание build-intents /10м + plan-render /5м (за флагом OFF).
  - **Пробелы до полного переключения (Шаг 6):** (1) Яндекс-таргетинг/discovery/found_urls в планировщик НЕ перенесены (v1 без тиров/персонализации — все интенты равны, рендер сразу); (2) lifecycle заявки (satisfied → терминальный статус) — сейчас заявка остаётся active, просто не даёт интентов; (3) глобальный суточный потолок; (4) score-веса (сейчас грубый порядок клиент/возраст).
  - Шаг 5 ⏳ shadow-замер; Шаг 6 ⏳ переключение (`EMAILS_GENERATE_ENABLED` off + `EMAILS_PLANNER_ENABLED` on) по доменам, после закрытия пробелов.
- **Фаза 3:** полная обратная связь (§7), авто-тюн cap, ретайр старых механизмов.

**Предусловие:** сначала стабилизировать репутацию (DKIM/DMARC + отлёжка сгоревших) — иначе эффект планировщика не измерить (смазано горелыми доменами).

## 10. Решения и открытые вопросы

**Решено (2026-07-09):**
- Каденс — **непрерывный** top-up к дневному бюджету ёмкости.
- Рендер HTML — **ленивый**, под запланированное.
- Гранулярность — **(request × supplier)**; покрытие и выпадение — **ПО ПОЗИЦИИ**; батчи ≤5 только как размер письма на рендере (не упреждающие).
- Стартовый cap нового адреса — **10**.
- Экспресс клиентских — **весом** (`w_customer`), без отдельного тракта.

**Ещё открыто:**
1. **Глобальный суточный потолок** платформы — нужен ли поверх пер-сендер/пер-получатель (страховка)?
2. **Веса** `w_*` в score — стартовые значения и где хранить (config vs таблица настроек).
3. **`offer_target` по умолчанию** — 4 глобально, или настраиваемо на заявку/категорию (плюс флаг `max_reach`).

---

# ПЕРЕДАЧА — СОСТОЯНИЕ НА 2026-07-10 (v2 В БОЮ)

**🟢 v2 переключён в бой 2026-07-10 ~12:10 Riga.** `.env` прода: `EMAILS_DAYPLAN_ENABLED=true`
(бэкап `.env.bak.dayplan-cutover`; там же `EMAILS_PLANNER_ENABLED=true`, `EMAILS_GENERATE_ENABLED=false`).
**Откат:** `EMAILS_DAYPLAN_ENABLED=false` + `config:clear` → plan-render вернётся в полный v1-режим.

## Что крутится
- `emails:plan-day` — расписание **07:30 МСК будни**, runInBackground. Строит план дня и рендерит
  его в `email_queue` (реализация модели v2 ниже). Тест-опции: `--dry-run`, `--request=`, `--limit=`,
  `--hold` (уводит отрендеренное в scheduled_at=2037 — диспетчер не заберёт), `--no-yandex`, `--force`.
- `emails:plan-render` — каждые 5 мин, АВТОМАТИЧЕСКИ в режиме top-up при включённом dayplan-флаге:
  рендерит только поставщиков моложе `EMAILS_DAYPLAN_TOPUP_NEW_HOURS` (деф. 24) — новых из discovery.
- `emails:build-intents` — как был (топит send_intents backlog).
- Discovery: кандидаты из Яндекс-выдачи plan-day → `DiscoverFromCampaignJob`
  (потолок `EMAILS_DAYPLAN_DISCOVERY_MAX=30`/прогон; флаг `EMAILS_DAYPLAN_DISCOVERY`).

## Компоненты v2 (все в бою)
- `DayPlanAssigner` — ядро-РАСПРЕДЕЛИТЕЛЬ (чистая логика): этап 1 — раунды по срочности,
  конверты (до daily_cap на получателя, деф. 10), подсадка в совместимые конверты; этап 2 —
  «липкие» ящики группами одинаковых наборов позиций (рендер-группы крупнее → меньше AI-вызовов),
  ротация 7д (senderRecent-пары), разные ящики конвертам одного получателя.
- `PlanDayCampaign` — пулы (grouper+selector − rir), Яндекс ПО ВСЕМ позициям с кэшем
  `dayplan:yx:item:{id}` (`EMAILS_DAYPLAN_YANDEX_CACHE_DAYS=3`; matchPool с текущим пулом свежий),
  аффинность gpt-4o (`EMAILS_DAYPLAN_AFFINITY_MODEL`; mini метила avoid ВСЕ пары — дробила письма),
  рендер группами (ящик+набор) → 1 батч/1 тело AI → token/body/EmailBuilder(wave=1)/Persister.
- `PositionCoverage` — activeItemIds / emailedSuppliersPerItem (считает ЛЮБУЮ rir-строку!) / remainingPool.

## Первый боевой прогон (2026-07-10)
395 батчей / 608 писем / **0 дедуп-скипов** / 14м45с; median 4 позиции/письмо; 245 получателей
(avg 2.5 конверта); discovery 182 кандидата → 30 в анализ. Релевантных конвертов ~0 — НЕ баг:
недельным позициям v1 уже написал релевантных (выпали из остаточного пула); на свежих заявках
релевантные пойдут первыми.

## Инциденты/грабли (не наступать)
- **Вечный SYN-SENT к ai.lazylift.ru**: Http `timeout(60)` НЕ обрывает зависший коннект — рендер
  стоял 25 мин. Фикс: `connectTimeout(15)` в `OpenAIClassifierClient` (commit a1eca8c).
- Прогон plan-day **идемпотентен**: kill + перезапуск безопасны (rir/капы учитывают отрендеренное).
- Тестовые письма отменять (`status='cancelled'`) И **удалять их rir-строки** — иначе
  emailedSuppliersPerItem навсегда выкинет поставщика из пула позиции.
- `php -r` по SSH ломается на бэкслешах — временные .php в /var/www/iqot (не /tmp).

## Мониторинг (первый авто-прогон — понедельник 07:30 МСК)
Лог `PlanDayCampaign: рендер` (queued/skipped/discovery), email_queue pending/sent от батчей дня,
дедуп-скипы (должны быть ~0), спам-гвард/блокировки ящиков, релевантные конверты на свежих заявках.

## Инцидент tomailbox.store (2026-07-13, разобран — commit 0a491b3)
6 ящиков домена tomailbox.store не могли слать (550 «send from only domains with BeGet MX»):
DNS-зона домена НЕ опубликована на NS Beget (делегирование на ns1/ns2.beget.com есть, но NS
зону не отдают — чинится в панели/поддержке Beget). Ошибка не матчила sender-side паттерны →
страйки ставились ПОЛУЧАТЕЛЯМ → 191 живой поставщик заблокирован, их rir сужали пулы v2
(«позиций дойдут до target» было 38%). Сделано: ящики отключены (block_reason, вернуть после
починки зоны), 191 получатель разблокирован, 753 rir-строки error-писем удалены; код-фиксы —
«BeGet MX» в isDeadMailbox (деактивация отправителя, без страйка получателю) +
PositionCoverage игнорирует rir писем error/failed/cancelled (кроме rir с не-pending статусом).
Эффект: target-прогноз плана 38% → 80%. ✅ ЗАКРЫТ: Beget опубликовал зону (13.07 ~15:00 МСК),
MX резолвится, пробная отправка с elki@tomailbox.store прошла (Message-ID вернулся), все 6
ящиков включены обратно (в т.ч. 3, которых успел автодеактивировать новый isDeadMailbox-фикс
на ретраях до починки — фикс сработал как задуман).

## Наблюдение (решение 2026-07-13: ничего не менять, смотрим)
- **Отклик**: НЕ судить по смешанной пятнице (v1 до 12:09 + v2 после). Честный тест — свежие
  заявки 449–456 (прошли v2 с нуля, письма ушли 13.07): замерить % ответов в среду–четверг
  (15–16.07) против типичных v1-дней (7.4–9.8%). День 13.07 искажён MX-инцидентом.
- **Плотность** (13.07: 42% одиночных, 36% четвёрок, median 2). Кандидаты на подкрутку ПОСЛЕ
  наблюдения: EMAILS_DAYPLAN_MAX_PER_EMAIL 4→8 (потолок дробит широких поставщиков: supplier
  110 получил 11 писем четвёрками) и/или ослабить AI-avoid (92 из 392 одиночных могли сесть в
  открытый конверт того же домена) вплоть до EMAILS_DAYPLAN_AFFINITY_AI=false.
- **Покрытие**: после разблокировки 191 получателя и возврата rir прогноз target-покрытия 38%→80% —
  проверить фактом по утреннему прогону 14.07 (лог `PlanDayCampaign: рендер`, дедуп-скипы ~0).

## Открытые хвосты
1. send_intents backlog для НЕ-новых поставщиков никем не дренится (plan-day интенты не трогает) —
   копится stale; почистить/ретайрить.
2. ✅ DNS-зона tomailbox.store починена Beget 13.07, отправка проверена, 6 ящиков включены.
3. Пробелы v1 остаются: lifecycle заявки (satisfied → терминальный статус), глобальный суточный
   потолок, score-веса срочности.
4. Ключевые коммиты: 09a360c (ядро-распределение), 4c2f0e1 (discovery+top-up+cutover),
   a1eca8c (липкие ящики+connectTimeout), db30f2d (рендер), 0114151 (расписание).

# МОДЕЛЬ v2 — ДНЕВНОЙ ПЛАНИРОВЩИК + TOP-UP (уточнено 2026-07-10 v2, ЦЕЛЕВАЯ)

v1 (непрерывный top-up) близорук: рендерит «здесь и сейчас» малыми данными → 93% батчей = 1 позиция, поставщик с несколькими позициями получает N одиночных писем. v2 планирует **на дневное окно** сразу по всей потребности и всем ресурсам.

## v2.1 Принципы
- **Раз в день** (утро, до окна) — тяжёлый прогон строит **план дня**. В течение дня план не пересобирается.
- **Задача РАСПРЕДЕЛЕНИЯ между двумя наборами**: позиции (признаки: остаточный пул, релевантность, совместимость, срочность) × поставщики (дневной лимит писем-«конвертов» на получателя, деф. 10). НЕ обход одного набора с раздачей из второго — сбалансированная раскладка позиций по конвертам.
- **Позиция не ограничивается искусственно**: её потребность = ВЕСЬ остаточный пул. Из рассылки дня её выводит только исчерпание пула либо ёмкости (хвост уйдёт завтра); заявка отзывается сама, когда КП собрано (позиции перестают быть «в работе»).
- **top-up в течение дня** — только добор появившихся из discovery НОВЫХ поставщиков по ещё-открытым позициям. Новые ЗАЯВКИ, пришедшие днём, ждут утра (лимиты дня расписаны + ждём заявки на совмещение позиций).

## v2.2 Утренний прогон (по шагам)
1. **Активные позиции** = все `request_item` с `офферов < offer_target` (новые + «на добор» — одно множество). Заявки completed/cancelled исключены.
2. **Пул на позицию (редуцированный):** профильные поставщики по таксономии `− уже писавшие ПО ЭТОЙ ПОЗИЦИИ` (по `request_item_responses`: строка = поставщику писали). Позиция **выпадает**, если пул исчерпан (всем писали).
3. **Яндекс-таргетинг: ПО ВСЕМ позициям (стадия планирования!)** → релевантная часть пула. Выдача (найденные домены) кэшируется на позицию (`dayplan_yandex_cache_days`, деф. 3) — повторные утра не дёргают Яндекс; матч с текущим пулом всегда свежий. Новые домены → discovery (TODO, пока не подключено).
4. **AI-аффинность (1 вызов на домен-группу):** позиции разных доменов не смешиваем; внутри домена совместимы, КРОМЕ avoid-пар от AI (несуразные сочетания — исключение, не правило; модель gpt-4o: mini чрезмерно дробит). Мягкая матрица, НЕ жёсткие группы.
5. **Назначатель-распределитель (ядро, `DayPlanAssigner`):**
   - У каждого поставщика — до `дневной лимит получателя` (деф. 10) **конвертов** (писем). Несовместимые позиции могут уйти одному поставщику в один день разными конвертами.
   - **Раунды** (равномерное покрытие, а не «первая срочная позиция съела всё»): в каждом раунде каждая активная позиция (в порядке срочности: дефицит+клиент+возраст) получает ПО ОДНОМУ новому поставщику из своего пула, **релевантные вперёд**.
   - Внутри поставщика: сначала **подсадка** в открытый конверт с совместимыми позициями и свободным местом (письмо плотнее, ёмкость не расходуется); иначе **новый конверт** — пока лимит получателя и ёмкость ящиков позволяют.
   - Конверт при открытии получает **ящик**: round-robin по остаткам дневных лимитов, ИСКЛЮЧАЯ ящики, писавшие этому получателю за окно ротации (7д), и уже назначенные другим конвертам этого получателя (иначе персистер молча срежет).
   - Результат = **упорядоченный план дня**: конверты с релевантными позициями в начало.
6. **Рендер** (та же команда): конверты группируются по (ящик, одинаковый набор позиций) → группа = один батч (1 тело AI, 1 токен, N поставщиков) → token/body/EmailBuilder(wave=1)/Persister → `email_queue`; дедуп `request_item_responses` на позицию×поставщика.

## v2.3 Дневной цикл (замкнутый)
Утро (план) → день (рассылка по плану + discovery + top-up новых поставщиков) → ночь (пришли КП: позиции добрали/закрылись; накопились новые заявки) → следующее утро (п.1 с обновлённым состоянием, редуцированные пулы).

## v2.4 Что меняем vs v1 (уже построенного)
- **Дедуп на уровень ПОЗИЦИИ** (сейчас на уровень заявки в `build-intents`): поставщик, писавший про позицию A, может получить письмо про новую активную позицию B, которую матчит. `PositionCoverage` расширить: emailed-suppliers per position, remaining-pool, isExhausted.
- **AI-аффинность** — новый сервис (1 вызов на очередь).
- **Назначатель** — новый сервис (распределение раундами по конвертам, ядро). Заменяет наивный per-product-type грайндер в `plan-render`.
- **Утренний прогон** — новая команда `emails:plan-day` (раз в сутки), делает шаги 1-6.
- **top-up** — `plan-render` переводим в режим «только добор новых поставщиков по открытым позициям».

## v2.5 Порядок реализации (за флагом, top-up v1 в бою пока)
1. `PositionCoverage` → позиционный дедуп/остаток пула/исчерпание.
2. `PositionAffinity` (AI, 1 прогон).
3. `DayPlanAssigner` (назначатель, ядро) + `emails:plan-day`.
4. Утренний прогон с Яндекс/discovery.
5. Переключить `plan-render` в режим top-up-новых.

# КАРМА / PROBATION ПУЛА ПОСТАВЩИКОВ (спека 2026-07-14)

## Проблема
Пул растёт: под каждую позицию `plan-day` добирает кандидатов из Яндекс-выдачи (discovery). Найденных надо **контролировать** — иначе пул раздувается мусором. По факту (диагностика 14.07): «профильный» пул под активные позиции = **1675** поставщиков, из них лишь **312 (19%) когда-либо прислали цену**; бо́льшая часть — `ai_inferred`/`null`-источник, не подтверждённые. Планировщик жжёт ёмкость на заведомо мёртвых.

## Что УЖЕ есть (внешний механизм — НЕ дублировать, см. memory `iqot-shared-coverage-mechanism`)
- **Хранение:** `supplier_product_types` (spt): `positive_signals`, `negative_signals`, `last_signal_at`, `is_included`, `source` (`ai_inferred`/`response_positive`/`response_negative`/`manual`/`null`), `needs_review`, `is_manual`.
- **Движок:** MySQL-процедуры `collect_response_signals` (оркестратор) → `process_offer_signals`(+) / `process_rejection_signal`(−) / `update_supplier_signal`; `get_suppliers_for_item` (отбор пула — процедурный, НЕ используется day-plan); `apply_review_decision`, `log_classification_signal`. Тела процедур недоступны (DEFINER `price_quotation_system@94.141.185.90`, IP утерян; прав на `mysql.proc` нет — тикет в Beget при необходимости).
- **Триггер:** n8n `Status Manager` (active), каждые 2ч, `CALL collect_response_signals()` + ведёт lifecycle заявок.
- **Поведение (реверс по данным + memory):** отклик → ±сигналы (avg positive у `response_positive`=3.3, у `ai_inferred`=**0.0**); порог заморозки — `negative_signals ≥ 5` И `positive_signals = 0` И не `is_manual` → `is_included=0` (один оффер защищает категорию даже при neg до 74). Негатив пишется ТОЛЬКО для `response_type=not_our_profile`. Новый `ai_inferred` → `is_included=1` **сразу**.

## Гэпы (то, чего в существующем механизме НЕТ — наша добавка)
1. **Probation-гейт новых кандидатов** — сейчас Яндекс/AI-находка валится в основной пул мгновенно, с 0 плюсов.
2. **thin/rich режим** — разное отношение к кандидату в бедных и проработанных темах.
3. **Яндекс-повтор → +карма** — карма чисто отклик-центрична; повторная релевантность не начисляется (подтверждено: `ai_inferred` avg_pos=0).

## Модель (единица = поставщик × product_type = строка spt)

### Состояния (выводим из существующих колонок + новый `pool_status`)
- **trusted** — в основном пуле, во ВСЕ матчащие позиции. Критерий: `source ∈ {manual, response_positive}` ИЛИ `karma ≥ promote_threshold`.
- **probation** — кандидат (`source ∈ {ai_inferred, yandex_discovery}`, кармы мало). В **rich**-темах: одноразовый (discovery пишет 1 раз под триггер-позицию, в общий пул НЕ входит). В **thin**: входит оптимистично, но «на карандаше».
- **frozen** — `negative_signals ≥ freeze_threshold` И не всплывал/не отвечал в окне. Исключён из пула, пока Яндекс снова не вытащит → unfreeze (сброс минусов). Уже реализовано процедурами (neg≥5 И pos=0 → is_included=0); переиспользуем.

### Сигналы кармы
| Событие | Дельта | Кто пишет |
|---|---|---|
| Прислал цену/КП | → **trusted** | процедуры (`response_positive`) ✅ |
| Отказ / бонс | → **frozen** | процедуры (`response_negative`, neg↑) ✅ |
| **Яндекс снова выдал домен по позиции** | **+1** | **НОВОЕ: plan-day (Laravel)** |
| Написали, но молчит И Яндекс не вытаскивал в окне | **−1** (decay) | **НОВОЕ: sweep-крон (Laravel)** |

**Асимметрия:** отклик — факт (сильный сигнал); Яндекс-повтор — гипотеза (слабый, +1); молчание → только заморозка (обратимая), НЕ удаление.

### Режим темы (переключатель thin/rich)
- `rich`, если `trusted`-поставщиков на product_type ≥ `regime_rich_min` (старт 30), иначе `thin`.
- **rich** (напр. лифтовые запчасти): кандидат `probation` **исключён** из общего пула day-plan; получает лишь одноразовый discovery-контакт; в основной пул — только по `karma ≥ promote_rich` (старт: 3 всплытия ИЛИ 1 отклик).
- **thin** (мало поставщиков): `probation` **включён** в пул оптимистично; заморозка по `freeze_thin` (старт 3 минуса); промоушен по `promote_thin` (старт 1).

## Точки интеграции (всё в Laravel, поверх колонок, что ведут процедуры)
1. **`CampaignSupplierSelector`** (читает пул day-plan): добавить гейт — `trusted` всегда; `probation` по режиму темы; `frozen` (is_included=0) исключён; **хард-дроп пустого/битого email** (55 строк — слать некуда).
2. **`PlanDayCampaign` Яндекс-стадия** (`matchPool`): найденный домен, попавший в `probation`/`frozen` поставщика → **+1 позитивный сигнал / unfreeze** (данные уже на руках на этой стадии). Домен вне базы → discovery (probation).
3. **Новый крон `emails:supplier-karma-sweep`** (раз в сутки): decay молчунов (−1), демоушен/заморозка, пересчёт rich/thin по product_type. НЕ дублировать отклик-карму (её ведут процедуры) — только Яндекс/decay/режимы.
4. **Backfill (разово):** `response_positive`+`manual` → trusted; `ai_inferred`/`null` без единого отклика → probation (в rich-темах выпадут из пула до подтверждения). Это и есть автоочистка задним числом — пул сожмётся до реального.

## Пороги (в config `services.email_planner`, тюним по факту)
`regime_rich_min=30`, `promote_rich=3`, `promote_thin=1`, `karma_decay_window_days`, `yandex_positive_weight=1`. Заморозку НЕ дублировать — её порог (`neg≥5 И pos=0`) держат процедуры; наш sweep только decay/Яндекс/режимы.

## Открытые вопросы
1. **Хранить `pool_status` отдельной колонкой** в spt или выводить из `source`+сигналов на лету? (колонка яснее, но пишем в общую с процедурами таблицу — координация.)
2. **Координация с процедурами:** decay/freeze мы считаем в Laravel, а процедуры — свою карму. Согласовать пороги `negative_signals`, чтобы не «качать» is_included в две стороны. Идеал (позже) — перенести весь карма-движок в Laravel, ретайрить процедуры + `Status Manager`.
3. **Тела процедур** — снять при доступе (Beget-тикет / DEFINER-хост), уточнить точную формулу текущей кармы перед координацией порогов.
