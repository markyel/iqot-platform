# Система промокодов и управление начальным балансом

## Обзор

Реализована полная система промокодов и управления начальным балансом для новых пользователей.

## Что реализовано

### 1. Управление первоначальным балансом

**Админка → Настройки системы** (`/manage/settings`)

- Добавлено поле "Стартовый баланс при регистрации"
- Сумма настраивается через веб-интерфейс
- Значение по умолчанию: 500 ₽
- При регистрации нового пользователя баланс берётся из этой настройки

**Файлы:**
- `app/Http/Controllers/Admin/SettingsController.php` - добавлена обработка `initial_balance`
- `resources/views/admin/settings/index.blade.php` - добавлено поле в форме
- `app/Http/Controllers/Auth/RegisteredUserController.php` - использование настройки при регистрации

### 2. Система промокодов

#### 2.1. База данных

**Таблица `promo_codes`:**
- `id` - ID промокода
- `code` - уникальный код (8 символов, заглавные буквы и цифры)
- `amount` - сумма промокода
- `is_used` - использован ли промокод
- `used_by_user_id` - кто использовал
- `used_at` - дата использования
- `description` - описание (для внутреннего учёта)

**Обновление таблицы `users`:**
- `promo_code_id` - ID использованного промокода
- `promo_code_activated_at` - дата активации
- `has_promo_priority` - приоритетное право на создание заявок

**Миграции:**
- `database/migrations/2026_01_27_000001_create_promo_codes_table.php`
- `database/migrations/2026_01_27_000002_add_promo_code_to_users_table.php`

#### 2.2. Административный интерфейс

**Управление промокодами** (`/manage/promo-codes`)

**Возможности:**
- Генерация пакетов промокодов (от 1 до 1000 штук)
- Указание суммы для каждого промокода
- Добавление описания для внутреннего учёта
- Просмотр списка всех промокодов
- Фильтрация по статусу (доступные / использованные)
- Поиск по коду
- Экспорт в CSV (доступные, использованные или все)
- Удаление неиспользованных промокодов
- Статистика: всего, использовано, доступно, общая сумма, активировано на

**Файлы:**
- `app/Http/Controllers/Admin/PromoCodeController.php` - контроллер управления
- `app/Models/PromoCode.php` - модель промокода
- `resources/views/admin/promo-codes/index.blade.php` - список промокодов
- `resources/views/admin/promo-codes/create.blade.php` - форма генерации
- `routes/web.php` - маршруты (строки 160-165)

#### 2.3. Активация промокода пользователем

**При регистрации:**
- Поле "Промокод" в форме регистрации
- Поддержка URL-параметра `?promo=CODE` для автозаполнения
- Валидация: проверка существования и доступности промокода
- Автоматическое начисление баланса при успешной активации

**В личном кабинете:**
- Раздел "Мой тариф" (`/cabinet/tariff`)
- Кнопка "Активировать промокод" (если ещё не активирован)
- Модальное окно для ввода кода
- Мгновенное пополнение баланса
- Уведомление об успешной активации

**Файлы:**
- `resources/views/auth/register.blade.php` - поле промокода в регистрации
- `app/Http/Controllers/Auth/RegisteredUserController.php` - обработка при регистрации
- `resources/views/cabinet/tariff/index.blade.php` - модальное окно активации
- `app/Http/Controllers/TariffController.php` - метод `applyPromoCode()`
- `routes/web.php` - маршрут `/cabinet/tariff/apply-promo-code` (строка 62)

#### 2.4. Отображение в админке

**Список пользователей** (`/manage/users`)
- Иконка 🎁 рядом с именем пользователя, если он активировал промокод
- Tooltip: "Использовал промокод"

**Файлы:**
- `resources/views/admin/users/index.blade.php` - обновлённая таблица пользователей

#### 2.5. Модель PromoCode

**Основные методы:**

```php
// Генерация уникального кода
PromoCode::generateCode(int $length = 8): string

// Проверка доступности
$promoCode->isAvailable(): bool

// Активация промокодом
$promoCode->activate(User $user): bool
```

**Связи:**
- `usedByUser()` - пользователь, активировавший промокод
- `promoCode()` в модели User - связь с промокодом

### 3. Интеграция с балансом

При активации промокода:
1. Баланс пользователя увеличивается на сумму промокода
2. Создаётся запись в `balance_transactions` с типом `promo_code`
3. Промокод помечается как использованный
4. Пользователь получает флаг `has_promo_priority = true`

## Использование

### Для администратора

1. **Настройка начального баланса:**
   - Перейти: Админка → Настройки системы
   - Изменить "Стартовый баланс при регистрации"
   - Сохранить

2. **Генерация промокодов:**
   - Перейти: Админка → Промокоды
   - Нажать "Сгенерировать промокоды"
   - Указать количество (1-1000) и сумму
   - Добавить описание (опционально)
   - Нажать "Сгенерировать"

3. **Экспорт промокодов:**
   - В списке промокодов нажать "Экспорт доступных"
   - Файл CSV будет скачан с кодами и информацией
   - Раздать коды пользователям

4. **Просмотр использования:**
   - В списке пользователей видны те, кто использовал промокод (иконка 🎁)
   - В списке промокодов видно, кто и когда активировал каждый код

### Для пользователя

1. **При регистрации:**
   - Ввести промокод в поле "Промокод (необязательно)"
   - Или перейти по ссылке вида `https://iqot.ru/register?promo=ABC12345`
   - Баланс будет автоматически пополнен при регистрации

2. **В личном кабинете:**
   - Перейти: Мой тариф
   - Нажать "Активировать промокод"
   - Ввести код
   - Баланс пополнится мгновенно

## URL-структура

### Административные маршруты
- `GET /manage/promo-codes` - список промокодов
- `GET /manage/promo-codes/create` - форма генерации
- `POST /manage/promo-codes` - генерация промокодов
- `DELETE /manage/promo-codes/{id}` - удаление промокода
- `GET /manage/promo-codes/export` - экспорт в CSV
- `GET /manage/settings` - настройки (включая начальный баланс)

### Пользовательские маршруты
- `POST /register` - регистрация с промокодом
- `POST /cabinet/tariff/apply-promo-code` - активация промокода

## Безопасность

- Промокод можно использовать только один раз
- Один пользователь может активировать только один промокод
- Коды генерируются случайно и уникальны
- Использованные промокоды нельзя удалить
- Все операции с балансом логируются в `balance_transactions`

## Технические детали

**Генерация кодов:**
- Длина: 8 символов
- Набор символов: A-Z (без O, I, L для читаемости) и 2-9
- Гарантия уникальности через проверку в БД

**Транзакции баланса:**
- Тип: `promo_code`
- Amount: отрицательное значение (пополнение)
- Description: "Активация промокода {CODE}"

**Приоритет пользователей:**
- Флаг `has_promo_priority` можно использовать для предоставления особых прав
- Например: приоритет в очереди обработки заявок
