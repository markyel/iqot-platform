# IQOT Platform

**Intelligent Quotation & Offer Tracking** — платформа автоматизации сбора и анализа коммерческих предложений.

## Архитектура

```
┌─────────────────────────────────────────────────────┐
│                    IQOT Platform                     │
├─────────────────────────────────────────────────────┤
│                                                      │
│   n8n (автоматизация)                               │
│     ├── Рассылка запросов поставщикам               │
│     ├── Сбор и парсинг ответов (AI)                 │
│     ├── Генерация отчётов                           │
│     └── Webhooks ←→ Laravel API                     │
│                                                      │
│   Laravel (веб-интерфейс)                           │
│     ├── Лендинг (/)                                 │
│     ├── Личный кабинет (/cabinet)                   │
│     ├── Админ-панель (/admin) — Filament            │
│     └── API (/api) — для n8n                        │
│                                                      │
│   MySQL (общая база данных)                         │
│                                                      │
└─────────────────────────────────────────────────────┘
```

## Требования к заявкам

Для того, чтобы заявка была принята в работу, необходимо соблюсти следующие требования:

### Общие требования
- **Тематика**: запчасти для лифтов и эскалаторов

### Контактная информация заявителя (обязательно)
- Название организации
- Адрес организации
- ИНН (10 или 12 цифр)
- КПП (9 цифр, опционально)
- ФИО ответственного сотрудника
- Телефон для связи

### Требования к позициям заявки
Для каждой позиции необходимо указать:
- **Полное и исчерпывающее название** товара
- **Тип оборудования**: лифт или эскалатор
- **Марка оборудования**
- **Артикул производителя**

## Системные требования

- PHP 8.2+
- Composer
- MySQL 8.0+ (или существующая БД от n8n)
- Node.js 18+ (для сборки assets)

## Установка

### 1. Клонирование и зависимости

```bash
cd /var/www
git clone <repo> iqot
cd iqot

# PHP зависимости
composer install

# Node зависимости
npm install
```

### 2. Конфигурация

```bash
# Копируем конфиг
cp .env.example .env

# Генерируем ключ
php artisan key:generate
```

Отредактируйте `.env`:

```env
APP_URL=https://iqot.ai

# Ваша MySQL база (та же, что использует n8n)
DB_HOST=127.0.0.1
DB_DATABASE=iqot
DB_USERNAME=iqot_user
DB_PASSWORD=your_password

# n8n вебхуки
N8N_WEBHOOK_URL=http://localhost:5678/webhook
```

### 3. База данных

```bash
# Если новая база
php artisan migrate

# Создать админа
php artisan make:filament-user
```

### 4. Сборка assets

```bash
# Для разработки
npm run dev

# Для продакшена
npm run build
```

### 5. Права доступа

```bash
chmod -R 775 storage bootstrap/cache
chown -R www-data:www-data storage bootstrap/cache
```

## Запуск

### Разработка

```bash
php artisan serve
# Откройте http://localhost:8000
```

### Продакшен (Nginx)

```nginx
server {
    listen 80;
    server_name iqot.ai;
    root /var/www/iqot/public;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    index index.php;

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
```

## 🎨 Design System

Платформа использует **IQOT Design System** — профессиональную систему дизайна для B2B приложений в лифтовой индустрии.

### Ключевые особенности

- 🎨 **Industrial Precision** — минималистичный, функциональный дизайн
- 📱 **Полная адаптивность** — от desktop до mobile
- 🔵 **Цветовая палитра** — Industrial Blue + Industrial Orange
- 🔤 **Типографика** — DM Sans (UI) + JetBrains Mono (коды)
- 🧩 **Blade компоненты** — готовые переиспользуемые элементы
- 🎯 **Lucide Icons** — современные SVG иконки

### Документация

- **[DESIGN_SYSTEM_README.md](./DESIGN_SYSTEM_README.md)** — обзор внедрения
- **[DESIGN_SYSTEM_USAGE.md](./DESIGN_SYSTEM_USAGE.md)** — полное руководство
- **[COMPONENTS_QUICK_REFERENCE.md](./COMPONENTS_QUICK_REFERENCE.md)** — быстрая справка

### Готовые компоненты

```blade
<!-- Badges для статусов -->
<x-badge type="completed">Завершено</x-badge>

<!-- Кнопки с иконками -->
<x-button variant="accent" icon="plus" :href="route('create')">
    Создать заявку
</x-button>

<!-- Page Header -->
<x-page-header title="Заголовок" description="Описание">
    <x-slot:actions>
        <x-button>Действие</x-button>
    </x-slot:actions>
</x-page-header>

<!-- Stat Cards -->
<x-stat-card value="42" label="Всего" icon="file-text" icon-type="primary" />

<!-- Empty State -->
<x-empty-state icon="inbox" title="Нет данных">
    <x-slot:action>
        <x-button>Создать</x-button>
    </x-slot:action>
</x-empty-state>
```

### Переоформленные страницы

- ✅ Layout с темным sidebar и responsive navigation
- ✅ Dashboard с stat cards и таблицами
- ✅ Управление заявками с фильтрами и пагинацией
- ✅ Все формы и инпуты по новому стилю

## Функциональность личного кабинета

### Создание заявок
- **AI-парсинг** текста заявки через n8n (распознавание позиций, брендов, артикулов)
- **Валидация** и редактирование позиций перед созданием
- **Система балансов** с заморозкой средств на время обработки заявки
- **Стоимость**: настраиваемая цена за позицию (по умолчанию 50 руб.)

### Отчеты по заявкам
- **Сравнительный анализ** КП от разных поставщиков
- **Автоматический расчет** лучших предложений
- **Экспорт в PDF** (планируется)
- **История изменений** статусов заявки

### Управление балансом
- Общий баланс и замороженные средства
- История транзакций
- Автоматическая разморозка при завершении заявки

## Структура проекта

```
iqot/
├── app/
│   ├── Http/Controllers/
│   │   ├── LandingController.php            # Лендинг
│   │   ├── CabinetController.php            # Личный кабинет
│   │   ├── UserRequestController.php        # Заявки пользователя
│   │   ├── Admin/
│   │   │   ├── RequestController.php        # Админ: модерация заявок
│   │   │   ├── ManageRequestController.php  # Админ: создание заявок (n8n)
│   │   │   ├── UserController.php           # Админ: пользователи
│   │   │   ├── UserSenderController.php     # Админ: настройка email
│   │   │   ├── SettingsController.php       # Админ: настройки
│   │   │   └── DiagnosticsController.php    # Админ: диагностика
│   │   └── Api/
│   │       └── WebhookController.php        # n8n вебхуки
│   ├── Models/
│   │   ├── User.php                         # Пользователи
│   │   ├── Request.php                      # Заявки (БД iqot)
│   │   ├── RequestItem.php                  # Позиции заявки
│   │   ├── BalanceHold.php                  # Заморозка баланса
│   │   ├── SystemSetting.php                # Настройки системы
│   │   ├── ClientOrganization.php           # Организации (БД reports)
│   │   ├── Category.php                     # Категории (БД reports)
│   │   ├── ProductType.php                  # Типы товаров (БД reports)
│   │   ├── ApplicationDomain.php            # Области применения (БД reports)
│   │   ├── ExternalRequest.php              # Заявки (БД reports)
│   │   ├── Supplier.php                     # Поставщики
│   │   └── Offer.php                        # КП
│   └── Services/
│       ├── N8nRequestService.php            # API для управления заявками (n8n)
│       ├── N8nParseService.php              # AI-парсинг через n8n
│       ├── N8nSenderService.php             # Отправка через n8n
│       └── RequestSyncService.php           # Синхронизация с БД reports
├── routes/
│   ├── web.php                           # Веб-роуты
│   └── api.php                           # API для n8n
├── resources/views/
│   ├── landing/                          # Лендинг
│   ├── cabinet/                          # Личный кабинет
│   ├── requests/                         # Заявки пользователей и отчеты
│   │   ├── create.blade.php              # Создание заявки
│   │   ├── index.blade.php               # Список заявок
│   │   ├── show.blade.php                # Просмотр заявки
│   │   └── report.blade.php              # Отчет по заявке
│   └── admin/
│       ├── requests/                     # Модерация заявок пользователей
│       └── manage/
│           └── requests/                 # Управление заявками через n8n
│               ├── index.blade.php       # Список заявок
│               ├── create.blade.php      # Создание с AI-парсингом
│               ├── edit.blade.php        # Редактирование
│               └── show.blade.php        # Просмотр
└── public/images/                        # Логотипы, иконки
```

## Функциональность админ-панели

### Управление заявками через n8n (новое!)

Администраторы могут создавать заявки на рассылку поставщикам:

- **Анонимные заявки** — без привязки к клиенту
- **Именные заявки** — для конкретной организации
- **AI-парсинг текста** — автоматическое распознавание позиций
- **Автоматическая рассылка** — каждые 60 минут через n8n

**Доступ:** `/manage/manage-requests` (требуется is_admin = true)

Подробная документация: [ADMIN_REQUESTS_SETUP.md](ADMIN_REQUESTS_SETUP.md)

## Маршруты приложения

### Личный кабинет (требуется авторизация)

```
GET  /cabinet                           # Дашборд
GET  /cabinet/settings                  # Настройки профиля
GET  /cabinet/requests                  # Мои заявки
GET  /cabinet/requests/create           # Создание заявки
POST /cabinet/requests/parse            # AI-парсинг текста
POST /cabinet/requests                  # Сохранение заявки
GET  /cabinet/requests/{id}             # Просмотр заявки
GET  /cabinet/requests/{id}/report      # Отчет по заявке
GET  /cabinet/balance/check             # Проверка баланса (AJAX)
```

### Админ-панель (требуется is_admin = true)

```
# Модерация заявок пользователей
GET  /manage/requests                   # Заявки от пользователей
GET  /manage/requests/{id}              # Детали заявки пользователя
POST /manage/requests/{id}/approve      # Одобрить заявку
POST /manage/requests/{id}/reject       # Отклонить заявку

# Управление заявками через n8n (новое!)
GET  /manage/manage-requests            # Список заявок из n8n
GET  /manage/manage-requests/create     # Создать заявку
POST /manage/manage-requests            # Сохранить заявку
GET  /manage/manage-requests/{id}       # Просмотр заявки
GET  /manage/manage-requests/{id}/edit  # Редактировать заявку
PUT  /manage/manage-requests/{id}       # Обновить заявку
POST /manage/manage-requests/{id}/cancel # Отменить заявку
POST /manage/manage-requests/parse-text # AI-парсинг (AJAX)

# Управление пользователями
GET  /manage/users                      # Пользователи
GET  /manage/users/{id}                 # Профиль пользователя
GET  /manage/users/{id}/sender/create   # Создать email-отправителя
POST /manage/users/{id}/sender          # Сохранить email-отправителя

# Настройки и диагностика
GET  /manage/settings                   # Настройки системы
GET  /manage/diagnostics                # Диагностика n8n
```

### API для n8n

#### Вебхуки (входящие от n8n)

```
POST /api/webhooks/request-update   # Обновление статуса заявки
POST /api/webhooks/offer-received   # Новое КП от поставщика
POST /api/webhooks/report-ready     # Отчёт готов
POST /api/webhooks/email-status     # Статус email
```

#### Защищённые эндпоинты (с Sanctum токеном)

```
GET  /api/requests                  # Список заявок
POST /api/requests                  # Создать заявку
GET  /api/suppliers                 # Список поставщиков
GET  /api/reports/{id}/pdf          # Скачать PDF
```

## Настройка системы

### Базовые настройки (через админ-панель)

Доступно по адресу `/admin/settings`:

- **price_per_item** — стоимость обработки одной позиции заявки (по умолчанию 50 руб.)
- **n8n_webhook_url** — URL вебхука n8n для отправки заявок
- **default_request_timeout** — таймаут обработки заявки (в часах)

### Email-отправители для пользователей

Для каждого пользователя админ должен настроить email-отправителя через `/admin/users/{id}/sender/create`:

- Почтовый сервер (host, port, encryption)
- Учетные данные (username, password)
- Данные организации (название, ИНН, КПП, адрес)
- Контактная информация

### Диагностика n8n

Проверка подключения к n8n доступна в `/admin/diagnostics`:

- Тестирование соединения
- Проверка вебхуков
- Логи взаимодействия

## Интеграция с n8n

Платформа использует две базы данных:

1. **iqot** (Laravel) — пользователи, баланс, заявки клиентов
2. **reports** (n8n) — обработанные заявки, предложения, поставщики

### Процесс обработки заявки

1. Пользователь создает заявку через `/cabinet/requests/create`
2. AI парсит текст через n8n (`N8nParseService`)
3. Заявка сохраняется в БД `iqot`, средства замораживаются
4. Заявка отправляется в n8n для рассылки поставщикам (`N8nSenderService`)
5. n8n создает запись в БД `reports` и рассылает email
6. Платформа синхронизирует статус через `RequestSyncService`
7. Когда КП получены, пользователь видит отчет в `/cabinet/requests/{id}/report`

### Синхронизация данных

Модель `ExternalRequest` работает с БД `reports` через отдельное подключение:

```php
protected $connection = 'reports';
```

Это позволяет получать данные о КП и поставщиках без дублирования.

## Очереди (для уведомлений)

```bash
# Запуск воркера
php artisan queue:work

# Или через Supervisor (продакшен)
```

## Лицензия

Proprietary © IQOT 2025
