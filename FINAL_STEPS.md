# ✅ Финальные шаги для запуска

## Что изменено

Изменен метод аутентификации n8n с `Authorization: Bearer` на `X-Auth-Token`.

Это должно решить проблему 403.

---

## Что нужно сделать

### Шаг 1: Убедитесь, что токен в .env

Откройте `.env` и проверьте:

```env
N8N_PARSE_AUTH_TOKEN=iqot_parse_api_2024_secret
```

### Шаг 2: Запустите батник обновления

Дважды кликните на:

```
update-and-test.bat
```

Он выполнит:
- Очистку кэша Laravel
- Проверку конфигурации
- Тестирование всех методов аутентификации

### Шаг 3: Откройте админку

```
http://ваш-домен/manage/manage-requests/create
```

### Шаг 4: Протестируйте AI-парсинг

1. В секции "AI-парсинг" введите:
   ```
   Кнопка вызова OTIS XAA123 10шт
   Датчик уровня KONE DEF456 5шт
   ```

2. Нажмите **"Распознать позиции"**

3. Должны появиться 2 позиции в таблице

---

## Если по-прежнему 403

### Вариант A: Токен в URL

Попробуйте передавать токен в URL. Измените в `app/Services/N8nRequestService.php` строку 99:

```php
// Было:
->post("{$this->baseUrl}/parse-request", [

// Стало:
->post("{$this->baseUrl}/parse-request?token={$this->parseAuthToken}", [
```

Затем:
```bash
php artisan config:clear
```

### Вариант B: Без аутентификации

Если webhook публичный, закомментируйте строки 92-95:

```php
// if ($this->parseAuthToken) {
//     $headers['X-Auth-Token'] = $this->parseAuthToken;
// }
```

Затем:
```bash
php artisan config:clear
```

---

## Проверка в n8n Cloud

1. Откройте n8n Cloud: https://liftway.app.n8n.cloud
2. Найдите workflow с вебхуком `parse-request`
3. Откройте узел "Webhook"
4. Проверьте настройки Authentication:

**Если настроено "Header Auth":**
- Header Name должен быть: `X-Auth-Token`
- Header Value: `{{$credentials.token}}` или напрямую токен

**Если настроено "Basic Auth" или другое:**
- Нужно изменить код или настройки webhook

---

## Альтернатива: Ручное добавление позиций

AI-парсинг - это удобство, но не обязательно!

1. Откройте `/manage/manage-requests/create`
2. Пропустите секцию AI-парсинга
3. Нажмите **"+ Добавить позицию"**
4. Заполните поля:
   - Название
   - Количество
   - Единица измерения
   - Категория
5. Создайте заявку

**Функционал создания заявок полностью работает без AI-парсинга!**

---

## Текущее состояние

✅ Backend полностью реализован
✅ Frontend интерфейсы готовы
✅ Маршруты настроены
✅ Метод аутентификации изменен на X-Auth-Token
✅ Справочники подключены
✅ Документация написана

⚠️ Нужно протестировать AI-парсинг с новым методом аутентификации

---

## Следующие действия

1. **Протестируйте создание заявки вручную** (без AI-парсинга)
   - Это покажет, что основной функционал работает

2. **Протестируйте AI-парсинг** после изменения метода аутентификации

3. **Если AI-парсинг не критичен** - можно временно скрыть эту секцию и использовать только ручное добавление

4. **Свяжитесь с администратором n8n** для проверки настроек webhook, если проблема сохраняется

---

## Полезные команды

```bash
# Очистка кэша
php artisan config:clear
php artisan cache:clear

# Проверка маршрутов
php artisan route:list | findstr manage-requests

# Проверка конфигурации
php artisan config:show services.n8n

# Тест аутентификации
php test-n8n-auth.php

# Полная диагностика
php check-setup.php
```

---

## Контакты для помощи

Если ничего не помогает:

1. Проверьте логи: `storage/logs/laravel.log`
2. Проверьте, что n8n доступен: `curl https://liftway.app.n8n.cloud/webhook/parse-request`
3. Свяжитесь с администратором n8n для проверки настроек аутентификации webhook

---

## 🎉 Успех!

Если всё работает:

1. Создайте первую заявку со статусом "Черновик" для теста
2. Проверьте, что она появилась в списке
3. Измените статус на "В работу" - она автоматически попадёт в рассылку (каждые 60 минут)

**Система готова к использованию!**
