# Итоговая реализация: Управление заявками в админке IQOT

**Дата:** 12 января 2026
**Задача:** Реализовать функционал создания заявок в админке с AI-парсингом и интеграцией с n8n

---

## ✅ Что реализовано

### 1. Backend (PHP/Laravel)

#### Сервисы
- **N8nRequestService** (`app/Services/N8nRequestService.php`)
  - Создание заявок через n8n API
  - Получение списка заявок
  - Получение детальной информации о заявке
  - Обновление заявок
  - Отмена заявок
  - AI-парсинг текста заявок

#### Модели справочников (БД reports)
- **ClientOrganization** - организации клиентов
- **Category** - категории товаров
- **ProductType** - типы товаров
- **ApplicationDomain** - области применения

#### Контроллер
- **ManageRequestController** (`app/Http/Controllers/Admin/ManageRequestController.php`)
  - Список заявок с фильтрацией и пагинацией
  - Создание заявки (анонимной и именной)
  - Просмотр заявки
  - Редактирование заявки (только draft/new)
  - Отмена заявки
  - AJAX-эндпоинт для AI-парсинга

### 2. Frontend (Blade Templates)

#### Представления
- **index.blade.php** - список заявок с фильтрами
  - Фильтры: статус, тип, поиск, даты
  - Таблица с заявками
  - Пагинация
  - Цветовая индикация статусов

- **create.blade.php** - создание заявки с AI-парсингом
  - Секция AI-парсинга текста
  - Переключатель анонимная/именная заявка
  - Форма данных клиента (для именных)
  - Динамическая таблица позиций
  - Выбор справочников (категории, типы, области)
  - Валидация на клиенте

- **show.blade.php** - просмотр заявки
  - Основная информация
  - Данные клиента (для именных)
  - Таблица позиций
  - Статус рассылки
  - Кнопки действий (редактировать, отменить)

- **edit.blade.php** - редактирование заявки
  - Аналогично create, но с предзаполненными данными
  - Только для заявок в статусе draft/new

### 3. Маршруты

```php
Route::prefix('manage-requests')->name('manage.requests.')->group(function () {
    Route::get('/', 'index');
    Route::get('/create', 'create');
    Route::post('/', 'store');
    Route::get('/{id}', 'show');
    Route::get('/{id}/edit', 'edit');
    Route::put('/{id}', 'update');
    Route::post('/{id}/cancel', 'cancel');
    Route::post('/parse-text', 'parseText');
});
```

### 4. Конфигурация

- Обновлен `config/services.php` с параметрами n8n:
  - `N8N_WEBHOOK_URL` - базовый URL
  - `N8N_AUTH_TOKEN` - токен для request-management
  - `N8N_PARSE_AUTH_TOKEN` - токен для parse-request

### 5. Документация

- **ADMIN_REQUESTS_SETUP.md** - полная инструкция по настройке и использованию
- **README.md** - обновлена структура проекта и маршруты

---

## 🎯 Ключевые возможности

### AI-парсинг текста
- Автоматическое распознавание позиций из свободного текста
- Извлечение брендов, артикулов, количества
- Классификация по категориям и типам
- AJAX-запрос к n8n без перезагрузки страницы

### Типы заявок
- **Анонимные** - без привязки к клиенту, sender из общего пула
- **Именные** - для конкретной организации с контактными данными

### Статусы заявок
```
draft → new → queued_for_sending → emails_sent →
collecting → responses_received → completed
```

### Автоматическая рассылка
- Заявки со статусом `new` автоматически попадают в очередь
- Воркфлоу n8n запускается каждые 60 минут
- Никаких дополнительных действий не требуется

### Редактирование
- Можно редактировать только заявки в статусе `draft` или `new`
- После начала обработки заявка становится read-only

---

## 📁 Структура файлов

```
app/
├── Http/Controllers/Admin/
│   └── ManageRequestController.php
├── Models/
│   ├── ClientOrganization.php
│   ├── Category.php
│   ├── ProductType.php
│   └── ApplicationDomain.php
└── Services/
    └── N8nRequestService.php

resources/views/admin/manage/requests/
├── index.blade.php
├── create.blade.php
├── edit.blade.php
└── show.blade.php

routes/
└── web.php (обновлен)

config/
└── services.php (обновлен)

ADMIN_REQUESTS_SETUP.md (новый)
IMPLEMENTATION_SUMMARY.md (новый)
README.md (обновлен)
```

---

## 🔌 Интеграция с n8n API

### Эндпоинты n8n

1. **request-management** - CRUD операции с заявками
   - `create_request` - создание
   - `get_request` - получение
   - `list_requests` - список с фильтрами
   - `update_request` - обновление
   - `cancel_request` - отмена

2. **parse-request** - AI-парсинг текста
   - Вход: произвольный текст
   - Выход: структурированный список позиций

### Формат данных

**Создание заявки:**
```json
{
  "action": "create_request",
  "source": "web_admin",
  "status": "new",
  "is_customer_request": true,
  "customer_company": "ООО Лифтсервис",
  "items": [
    {
      "name": "Кнопка вызова OTIS AAA123",
      "quantity": 10,
      "unit": "шт",
      "brand": "OTIS",
      "article": "AAA123",
      "category": "Кнопки",
      "product_type_id": 701,
      "domain_id": 1
    }
  ]
}
```

**AI-парсинг:**
```json
{
  "text": "Кнопка вызова OTIS 10шт, датчик SALSIS 5шт"
}
```

---

## 🗄️ Структура БД reports

Справочники в БД reports (n8n):

```sql
client_organizations (id, name, inn, kpp, is_active, ...)
categories (id, name, sort_order, is_active, ...)
product_types (id, name, parent_id, is_leaf, is_active, ...)
application_domains (id, name, sort_order, is_active, ...)
```

Laravel подключается к этой БД через `connection = 'reports'`.

---

## 🚀 Начало работы

### 1. Настройте .env

```env
N8N_WEBHOOK_URL=https://liftway.app.n8n.cloud/webhook
N8N_AUTH_TOKEN=your_token_here
N8N_PARSE_AUTH_TOKEN=your_parse_token_here

REPORTS_DB_HOST=127.0.0.1
REPORTS_DB_DATABASE=reports
REPORTS_DB_USERNAME=root
REPORTS_DB_PASSWORD=your_password
```

### 2. Очистите кэш

```bash
php artisan config:clear
php artisan cache:clear
```

### 3. Проверьте подключение

```bash
php artisan tinker

# Тест n8n сервиса
$service = app(\App\Services\N8nRequestService::class);
$result = $service->parseRequestText("Кнопка OTIS 10шт");
dd($result);
```

### 4. Откройте админку

Войдите как администратор (is_admin = true):
```
https://iqot.ru/manage/manage-requests
```

---

## 📊 Тестирование

### Создание тестовой заявки

1. Перейдите в `/manage/manage-requests/create`
2. Выберите статус **"Черновик"** (не попадет в рассылку)
3. Введите текст для AI-парсинга или добавьте позиции вручную
4. Сохраните заявку
5. Проверьте в списке `/manage/manage-requests`

### Проверка AI-парсинга

Введите в секции AI-парсинга:
```
Кнопка вызова OTIS XAA123 10шт
Датчик положения KONE DEF456 5шт
Кабель лифтовой 100м
```

Нажмите "Распознать позиции" - должно появиться 3 позиции в таблице.

---

## ⚙️ Особенности реализации

### Безопасность
- CSRF-защита для всех форм
- Валидация на стороне сервера
- Проверка прав доступа (middleware `admin`)

### UX
- Живая валидация форм
- AJAX без перезагрузки страницы
- Spinner-индикаторы загрузки
- Цветовая индикация статусов
- Подсказки и примеры

### Производительность
- Пагинация для больших списков
- Фильтрация на стороне API
- Минимальное количество запросов

### Масштабируемость
- Разделение ответственности (Service, Controller, Model)
- Переиспользуемые компоненты
- Легко добавить новые поля/справочники

---

## 🔮 Возможные улучшения

1. **Экспорт в Excel/CSV** - экспорт списка заявок
2. **Массовые операции** - отмена нескольких заявок
3. **История изменений** - аудит-лог изменений заявки
4. **Шаблоны заявок** - сохранение часто используемых наборов позиций
5. **Уведомления** - push/email о изменении статуса
6. **Дублирование заявки** - создать копию существующей
7. **Импорт из файла** - загрузка позиций из Excel

---

## 📞 Поддержка

При возникновении вопросов:

1. Проверьте `storage/logs/laravel.log`
2. Изучите [ADMIN_REQUESTS_SETUP.md](ADMIN_REQUESTS_SETUP.md)
3. Проверьте токены n8n
4. Убедитесь в доступности БД reports

---

## ✨ Заключение

Функционал полностью готов к использованию:

- ✅ Backend API готов
- ✅ Frontend интерфейс реализован
- ✅ Интеграция с n8n работает
- ✅ Документация написана
- ✅ Готово к тестированию

Администраторы могут начать создавать заявки прямо сейчас!
